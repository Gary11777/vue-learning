{
  "scripts": {
    "serve": "npx serve",
  "api": "json-server db.json -p 3001",
  "start": "concurrently \"npm run serve\" \"npm run api\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "json-server": "^1.0.0-beta.3"
  }
}
